client_configs:
  train_configs:
    # Local trainer
    trainer: "VanillaTrainer"
    mode: "step"
    num_local_steps: 10
    optim: "Adam"
    optim_args:
      lr: 0.001
    # Loss function
    loss_fn_path: "./resources/loss/flamby_ixi.py"
    loss_fn_name: "BaselineLoss"
    # Client validation
    do_validation: True
    do_pre_validation: True
    metric_path: "./resources/metric/flamby_ixi.py"
    metric_name: "metric"
    # Differential privacy
    use_dp: False
    epsilon: 1
    clip_grad: False
    clip_value: 1
    clip_norm: 1
    # Data loader
    train_batch_size: 2
    val_batch_size: 2
    train_data_shuffle: True
    val_data_shuffle: False
  
  data_readiness_configs:
    generate_dr_report: True
    output_dirname: "./output"
    output_filename: "data_readiness_report"
    fairness_feature_idx: 1
    dr_metrics: 
      class_imbalance: False
      sample_size: True
      num_classes: False
      data_shape: True
      completeness: False
      data_range: True
      overall_sparsity: True
      variance: True
      skewness: True
      entropy: True
      kurtosis: True
      class_distribution: False
      brisque: False
      sharpness: False
      outlier_proportion: False
      time_to_event_imbalance: False
      plot:
        class_distribution_plot: False
        segmentation_class_distribution_plot: True
        data_sample_plot: True
        data_distribution_plot: False
        class_variance_plot: False
        feature_correlation_plot: False
        feature_statistics_plot: False
        outlier_detection_plot: False
        representative_rates_plot: False
        feature_importance_plot: False
        incompleteness_plot: False
      combine:
        feature_space_distribution: False
    
  model_configs:
    model_path: "./resources/model/flamby_ixi.py"
    model_name: "Baseline"
    # model_kwargs:
    #   input_size: 39
      # num_classes: 1
  
  comm_configs:
    compressor_configs:
      enable_compression: False
      # Used if enable_compression is True
      lossy_compressor:  "SZ2Compressor"
      lossless_compressor: "blosc"
      error_bounding_mode: "REL"
      error_bound: 1e-3
      param_cutoff: 1024

server_configs:
  scheduler: "SyncScheduler"
  scheduler_kwargs:
    num_clients: 3
    same_init_model: True
  aggregator: "FedAvgAggregator"
  aggregator_kwargs:
    client_weights_mode: "equal"
  device: "cpu"
  num_global_epochs: 10
  logging_output_dirname: "./output"
  logging_output_filename: "result"
  comm_configs:
    grpc_configs:
      server_uri: localhost:50051
      max_message_size: 1048576
      use_ssl: False