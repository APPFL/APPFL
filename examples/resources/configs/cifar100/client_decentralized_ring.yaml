client_id: "Client1"
train_configs:
  # Device
  device: "cuda"
  # Logging and outputs
  logging_output_dirname: "./output"
  logging_output_filename: "result_decentralized_ring"

# Local dataset (CIFAR-100, IID class-balanced partition)
data_configs:
  dataset_path: "./resources/dataset/cifar100_dimat.py"
  dataset_name: "get_cifar100"
  dataset_kwargs:
    num_clients: 5
    client_id: 0

comm_configs:
  grpc_configs:
    server_uri: localhost:50051
    max_message_size: 1048576
    use_ssl: False
