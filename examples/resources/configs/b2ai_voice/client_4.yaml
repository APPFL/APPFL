client_id: "Client3"

train_configs:
  device: "cpu"
  logging_output_dirname: "./output"
  logging_output_filename: "result"

# Local dataset -- cohort: Voice Disorders
# Validation is the shared multi-cohort / controls set (client_4/data.npz)
data_configs:
  dataset_path: "./resources/dataset/b2ai_voice_dataset.py"
  dataset_name: "get_b2ai_voice"
  dataset_kwargs:
    client_id: 3
    dataset_path: "./datasets/RawData/b2ai-voice/partitioned_data"

comm_configs:
  grpc_configs:
    server_uri: localhost:50051
    max_message_size: 1048576
    use_ssl: False
